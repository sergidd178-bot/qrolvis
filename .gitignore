# dependencias
/node_modules
/.pnp
.pnp.*

# Next.js
/.next/
/out/
next-env.d.ts

# producción
/build

# entorno
#
# Se ignora cualquier .env, incluidos los que genera el CLI de Vercel
# (.env.local con VERCEL_OIDC_TOKEN). Ahí viven las claves reales.
.env*

# ...salvo la plantilla, que SÍ se versiona a propósito: documenta qué
# variables hacen falta y no contiene ningún valor. La negación es explícita
# para que nadie la borre pensando que se coló.
!.env.example

# despliegue
.vercel

# TypeScript
*.tsbuildinfo

# sistema operativo
.DS_Store
Thumbs.db

# registros
npm-debug.log*
yarn-debug.log*
yarn-error.log*
.pnpm-debug.log*
