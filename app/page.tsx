// Marcador de posición. La raíz no forma parte de ningún flujo de la versión 1:
// el consumidor entra por /f/[code] y el operador por /admin.
export default function HomePage() {
  return <main>Qrolvis</main>;
}
